# Python virtual environments
.venv/
venv/
env/

# Python caches
__pycache__/
*.py[cod]
*$py.class

# Test/cache artifacts
.pytest_cache/
pytest-cache-files-*/
.coverage
htmlcov/

# Environment and local secrets
.env
.env.*
!.env.example
.user.yml

# Local temp/log files
.tmp/
tmp/
logs/
*.log

# dbt artifacts
target/
dbt_packages/
logs/dbt.log

# Elementary (edr) artifacts — scratch dir + generated HTML report (5MB+, regenerated
# on demand). Keep packages.yml / package-lock.yml tracked (they pin the dbt deps).
edr_target/
bitcoin_pipeline/reports/

# Local data artifacts
bitcoin_pipeline/data/bronze/
bitcoin_pipeline/data/**/*.parquet
bitcoin_pipeline/data/**/*.duckdb
bitcoin_pipeline/data/**/*.duckdb.wal

# Streamlit local state
.streamlit/secrets.toml

# OS/editor files
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Local agent instructions
AGENTS.md
CLAUDE.md

# Private working docs (plans, explanations, notes) — never publish to the repo
docs/
